
We are going to be adding a new component components/about named testimonials 

You will be adding this to lib/utils.ts 

import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

and using components/ui/animated-testimonialst.tsx, you will pull from testimonials.json in /data 

also for changes for the component i want the outside of the bento to have square corners, and the photo bento should be 
in black and white and only color on hover, then make the arrow buttons square as well. 

you are going to add it under the themeimagecompare. 