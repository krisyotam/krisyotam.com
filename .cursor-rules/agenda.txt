
# Redo of the app/docs directory 
We are going to be doing a fairly comprehensive redesign of app/docs 

first to explain how the new route will be structured 

app/docs
  - [type] 
    - [category]
      - [slug]

the data for these will be in data/docs where you will fine types.json, categories.json, and docs.json
note the above files have demo data but that is fine for now. 

Also create a app/docs/content directory that won't be in use immeditely right now but more later. The Current slug page is perfectly fine 
so for now just correct that route. 













Ignore Below This Line
---
# Creation of New Lab Page 
Important Notes: the lab page itself will not need a password to access, however some notebooks may have a password 
some may not have a password. A category in categories.json is referred to as a notebook, so when changing notebook 
via the category filter (when it changes the url) it will then ask for the password. 






# Creation of Sequences Page 
- create a unified feed of all post files 
  - data/app/papers
  - data/app/papers
  - data/app/papers
  - data/app/papers
  - data/app/papers
  - data/app/papers
  - data/app/papers
  - data/app/papers
- create a feed of all category files 
- create a unified feed of all tags files 
