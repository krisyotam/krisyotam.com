BUILD ERRORS TO FIX:

1. Syntax error in file: app/docs/[type]/[category]/[slug]/page.tsx

   Error details:
   - Expression expected at line 60
   - Code appears to have misplaced code segments:
     ```
     }   description: item.description,
        };
     }
     ```
   
   This suggests there's a function with improperly placed code inside it.
   The error appears between lines 58-62:
     58 |   if (!doc) return notFound();
     59 |   return <DocDetail doc={doc} />;
     60 | }   description: item.description,
     61 |   };
     62 | }
   
   The object property `description: item.description` is incorrectly placed after a function closing brace.

2. Package manager inconsistency:
   - Warning about both package-lock.json (npm) and yarn.lock files being present
   - "Skipping build cache since Package Manager changed from "npm" to "yarn""
   - Consider removing package-lock.json to avoid resolution inconsistencies

3. Dependency warnings:
   - Multiple peer dependency warnings for:
     - @types/next > @types/next-server > next-server@9.0.5
     - react-day-picker@8.10.1
   - Multiple "Workspaces can only be enabled in private projects" warnings